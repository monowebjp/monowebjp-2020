{
  "sample": {
    "heading": [
      {
        "lv": "{{lv}}",
        "tag": "{{tag}}",
        "name": "{{name}}",
        "utility": "{{utility}}"
      }
    ],
    "icon": [
      {
        "name": "{{name}}"
      }
    ]
  }
}