<%
if (typeof data === 'undefined') {
    var data = sample.heading
}
data.forEach(function (item) {
%>
    <h<%= item.tag %> class="mjpc-heading mjpc-heading--lv<%= item.lv %> <%= item.utility %>">
        <%= item.name %>
    </h<%= item.tag %>>
<% }); %>